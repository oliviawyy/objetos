// function somar(){
//     let v1 = 2;
//     let v2 = 2;
//     const resultado = v1 + v2;
//     console.log(resultado);
// }

// somar();

// const soma = somar



// parametros para o calculo
function somar(v1, v2){
    const resultado = v1 +
}

// retornando o valor
function somar(v1, v2){
    const resultado = v1 + v2;
    return resultado
}

// retornando apenas o valor informado no parametro
function somar(v1, v2){
    return(v1 + v2)
}


// 01 - crie uma funcao que retorne a porcentagem de um valor
function calcPorcentagem(valor,taxa){
    return valor * (taxa / 100);
}




// 02 - crie uma funcao que retorne um valor de uma compra a prazo
let calcPagPrazo = (valor, taxa) =>{
    return valor + (valor * taxa / 100)
}


// 03




// resolucao com funcao de seta(arrow function)
let calcPorcantagem = (valor, taxa) =>{
    return valor * (taxa / 100);
}

